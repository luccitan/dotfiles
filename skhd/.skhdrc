#  NOTE(koekeishiya):
#  A list of all built-in modifier and literal keywords can
#  be found at https://github.com/koekeishiya/skhd/issues/1
#
# A hotkey is written according to the following rules:
# hotkey   = <keysym> ':' <command> | <keysym> '->' ':' <command>
#
# keysym   = <mod> '-' <key> | <key>
# mod      = 'built-in mod keyword' | <mod> '+' <mod>
# key      = <literal> | <keycode>
# literal  = 'single letter or built-in keyword'
# keycode  = 'apple keyboard kVK_<Key> values (0x3C)'
# ->       = keypress is not consumed by skhd
#
# command  = command is executed through '$SHELL -c' and
#  follows valid shell syntax. if the $SHELL environment
#  variable is not set, it will default to '/bin/bash'.
#  when bash is used, the ';' delimeter can be specified
#  to chain commands.
#
#  to allow a command to extend into multiple lines,
#  prepend '\' at the end of the previous line.
#
#  an EOL character signifies the end of the bind.

# open terminal, blazingly fast compared to iTerm/Hyper
# cmd - return : open -na /Applications/Kitty.app

# move window
fn - z : chunkc tiling::window --warp north
fn - q : chunkc tiling::window --warp west
fn - s : chunkc tiling::window --warp south
fn - d : chunkc tiling::window --warp east

# send window to desktop and follow focus
fn + shift - a : chunkc tiling::window --send-to-monitor prev;\
                 chunkc tiling::monitor -f prev
fn + shift - e : chunkc tiling::window --send-to-monitor next;\
                 chunkc tiling::monitor -f next

# mirror tree y-axis
fn - a : chunkc tiling::desktop --mirror vertical

# mirror tree x-axis
fn - e : chunkc tiling::desktop --mirror horizontal

# toggle window fullscreen
fn - p : chunkc tiling::window --toggle fullscreen

# float / unfloat window
fn - i : chunkc tiling::window --toggle float

# increase region size
fn - o : chunkc tiling::window --use-temporary-ratio 0.05 --adjust-window-edge north
fn - k : chunkc tiling::window --use-temporary-ratio 0.05 --adjust-window-edge west
fn - l : chunkc tiling::window --use-temporary-ratio 0.05 --adjust-window-edge south
fn - m : chunkc tiling::window --use-temporary-ratio 0.05 --adjust-window-edge east

# decrease region size
fn + shift - o : chunkc tiling::window --use-temporary-ratio -0.05 --adjust-window-edge south
fn + shift - k : chunkc tiling::window --use-temporary-ratio -0.05 --adjust-window-edge east
fn + shift - l : chunkc tiling::window --use-temporary-ratio -0.05 --adjust-window-edge north
fn + shift - m : chunkc tiling::window --use-temporary-ratio -0.05 --adjust-window-edge west
